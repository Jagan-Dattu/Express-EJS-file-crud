<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Document</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    .floating {
      position: absolute;
      border-radius: 9999px;
      opacity: 0.3;
      animation: float 8s ease-in-out infinite;
    }

    @keyframes float {
      0%   { transform: translateY(0); }
      50%  { transform: translateY(-20px); }
      100% { transform: translateY(0); }
    }

    .float-delay-1 { animation-delay: -2s; }
    .float-delay-2 { animation-delay: -3s; }
    .float-delay-3 { animation-delay: -4s; }
  </style>
</head>
<body class="relative bg-gray-100 p-6 overflow-hidden">

  <div class="relative max-w-xl mx-auto mt-6 bg-white p-6 rounded shadow z-10">
    <a class="text-s font-bold mb-2 bg-blue-300 px-3 py-1 inline-block rounded" href="/">← Go Back To Home</a>

    <h2 class="text-xl font-bold mt-4 mb-2"><%= filename %></h2>  
    <p class="text-gray-700 mb-4 whitespace-pre-line"><%= filedata %></p>  

    <div class="flex gap-4">
      <form action="/edit/<%= filename %>" method="get">
        <button type="submit" class="bg-yellow-400 text-black px-4 py-2 rounded hover:bg-yellow-500">Edit</button>
      </form>

      <form action="/delete/<%= filename %>" method="post" onsubmit="return confirm('Are you sure you want to delete this note?');">
        <button type="submit" class="bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600">Delete</button>
      </form>
    </div>
  </div>

</body>
</html>
