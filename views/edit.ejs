<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 p-6">
  <div class="max-w-xl mx-auto bg-white p-6 rounded shadow">
    <a class="text-s font-bold mb-4 inline-block bg-blue-300 px-3 py-1 rounded" href="/">← Back</a>

    <form action="/edit/<%= filename %>" method="post" class="space-y-4">
      <input type="text" name="title" value="<%= filename.replace('.txt', '') %>" class="w-full p-2 border rounded" required>
      <textarea name="content" rows="6" class="w-full p-2 border rounded" required><%= content %></textarea>
      <button type="submit" class="bg-green-500 text-white px-4 py-2 rounded hover:bg-green-600">Save Changes</button>
    </form>
  </div>
</body>
</html>
